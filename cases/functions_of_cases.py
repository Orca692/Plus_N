# Здесь хранятся функции операторов, которые исполняет программа

import os # clear()
clear = lambda: os.system('cls') # хз что значит, скопировал для clear()

def case_(arg): # case_
    print(arg + ': not found')

def flow(In_Flow):
    print(In_Flow)